<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:dist="http://schemas.android.com/apk/distribution"
        package="com.xp.sample_payment">

    <dist:module
            dist:instant="false"
            dist:title="@string/title_sample_payment">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity android:name=".StartFlowPayment">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="start_payment_flow" />
            </intent-filter>
        </activity>
    </application>

</manifest>